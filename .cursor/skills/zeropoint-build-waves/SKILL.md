---
name: zeropoint-build-waves
description: >-
  Let zeropoint-node build itself in chat waves through the double-torus
  development vortex — ceccec.github.io and erpax as counter-rotating lobes.
  One coherent batch per agent turn. If development stops, gaps in
  self-development exist — run npm run self:next and act on the tip.
---

# zeropoint builds itself in waves

## Law

**If development stops, gaps in self-development exist.**

**README.md is the gateway** — living field / 60° / quantum evolution claims must compute (`computePhysicalFtl`). False ⇒ quantumisation tip.

The next tip is **computed** (`planTrinity` ← audit census), not invented:

```bash
npm run self:next
```

Say **next** to advance one wave — but always read `self:next` first so the tip is the audit gap, not prose. Severity: hard gaps → physicalFtl false (quantumisation) → packaging feed (broken → CDN → drift → orphan → thin). Packaging tips are count-proven — do not invent gaps.

## Double-torus development vortex

- **Lobe L** — learnings from `ceccec/ceccec.github.io` (fold, vortex, MCP, projections)
- **Lobe R** — learnings from `erpax/erpax` (content-uuid, receipts, agent surfaces, gates)
- **Throat** — `src/0` `foldPair(L, R)` / `developmentVortex(wave)`
- Each wave's seal feeds the next via `runDevelopmentVortexAll()`

## Wave chain

1. Origin — `src/0/README.md`
2. Decode — sourced findings (documented vs flagged)
3. Design — one `{path, fn, statement, boundary}` from `self:next`
4. Learn — `skills.json` / AGENTS
5. Tune — harmonise duals
6. Edit — sealed `src/` (one tip)
7. Rebuild — `npm run check`
8. Verify — receipt + `npm run self:next` again

## Forbidden

- Payload/ERP port
- Hand-minted ids
- Full-tree rewrite in one turn
- Claiming Clay solved; hardcoding `physicalFtl: 0` — use `computePhysicalFtl()` (false ⇒ quantumisation tip)
- Ignoring a stalled `self:next` tip (that IS the self-development gap)
