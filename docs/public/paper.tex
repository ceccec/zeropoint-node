% Generated by scripts/paper-gen.mjs — do not edit.
% Every figure is read from the source at generation time; paper:check fails on
% a byte of drift. The HTML at docs/public/paper.html is the same document in
% MathML, from the same generator and the same numbers.
\documentclass[11pt,a4paper]{article}
\usepackage[margin=25mm]{geometry}
\usepackage{amsmath,amssymb,booktabs,microtype}
\title{Exact arithmetic over a ten-digit space}
\author{zeropoint-node 1.5.1}
\date{}
\begin{document}
\maketitle

\begin{abstract}
This paper sets out the arithmetic that \texttt{zeropoint-node} implements and the
predicates that decide it. The digit space is partitioned by a doubling map into a
six-element orbit and a three-element axis; frequency and hue are one integer read in
two units; ratios are carried as pairs of integers rather than decimals. 33 of
33 theorems carry a predicate that runs and holds, and 39 of
63 statements in the Lean files are accepted by the Lean kernel.
3 claims are recorded as axioms because no finite computation decides them.
\textbf{How much of the package those predicates hold:} 1411 of
1751 exported values are reachable from no law at all (a floor, not a total);
of 57 exported literal constants 8 are forced and
49 are held by nothing; and 9 of the 33 predicates are
pinned rather than forced. Measured, not asserted.
\textbf{No physical experiment is reported here.}
\end{abstract}

\begin{equation}
\mathrm{dr}(n) = 1 + \bigl((n-1) \bmod 9\bigr), \qquad n \geq 1
\end{equation}
\noindent\small The digital root, as a closed form rather than an iteration. It is idempotent, and invariant under adding nine.\normalsize

\begin{equation}
d_{k+1} = \mathrm{dr}(2 d_k)
\end{equation}
\noindent\small Doubling inside the digital root. From the walk is 1 \$\to\$ 2 \$\to\$ 4 \$\to\$ 8 \$\to\$ 7 \$\to\$ 5 and closes after six.\normalsize

\begin{equation}
O = \{1,2,4,8,7,5\}, \qquad A = \{3,6,9\}, \qquad O \cap A = \varnothing
\end{equation}
\noindent\small The orbit and the axis partition the non-zero digits: six on the doubling ring, three on the axis, and the void makes ten.\normalsize

\begin{equation}
v(n) = 1 - (n \bmod 9)
\end{equation}
\noindent\small The through-void involution. It is its own inverse, and 5 is its only fixed point.\normalsize

\begin{equation}
f(d) = \frac{432\,d}{12} = 36 d, \qquad d \in \{0,\dots,9\}
\end{equation}
\noindent\small Frequency in hertz. The exact form is ; the implementation evaluates and so returns 252.00000000000003 at where the exact value is 252. The discrepancy is binary floating point, not arithmetic, and it is stated here rather than rounded away.\normalsize

\begin{equation}
f_{\mathrm{oct}}(n) = 432 \cdot 2^{n}
\end{equation}
\noindent\small Octave scaling. holds exactly, because the factor is two.\normalsize

\begin{equation}
h(d) = 36 d \bmod 360
\end{equation}
\noindent\small Hue in degrees. Colour and pitch carry the same integer: this is an identity of two definitions, not an empirical correspondence.\normalsize

\begin{equation}
\varphi = \frac{1+\sqrt{5}}{2}, \qquad \varphi^{2} = \varphi + 1
\end{equation}
\noindent\small The golden ratio and its defining equation. Computed here as 1.618033988749895.\normalsize

\begin{equation}
f_c = \frac{432\cdot 3}{2},\quad f_m = \frac{432\cdot 6}{5},\quad f_y = \frac{432\cdot 9}{5},\quad f_k = \frac{432\cdot 1}{3}
\end{equation}
\noindent\small The four channel ratios, held as exact fractions rather than decimals: cyan 1296/2, magenta 2592/5, yellow 3888/5, key 432/3. Two of them do not terminate in decimal, which is the reason the representation is a pair of integers.\normalsize

\begin{equation}
\mathrm{med}\!\left(\frac{a_1}{b_1},\frac{a_2}{b_2}\right) = \frac{a_1+a_2}{b_1+b_2}
\end{equation}
\noindent\small The mediant, which lies strictly between its arguments and is not their mean: while the mean is 5/12. Computed here as 2/5.\normalsize

\begin{equation}
H = \frac{1}{\sqrt{2}}\begin{bmatrix} 1 & 1 \\ 1 & -1 \end{bmatrix}, \qquad H^{2} = I
\end{equation}
\noindent\small The Hadamard gate and its involution. The seal decides this by applying it twice to each basis state of one qubit; linearity makes two states exhaustive.\normalsize

\begin{equation}
\sum_i |a_i|^{2} = 1
\end{equation}
\noindent\small The Born rule as a normalisation condition. Every unitary step in the simulator preserves it, and the tensor product of two normalised registers is normalised.\normalsize

\begin{equation}
E^{\circ} = \frac{\Delta G}{nF}
\end{equation}
\noindent\small The reversible cell potential. With and C\cdot mol&\#8315;&\#185;, this is 1228899 \$\mu\$V, from \$\Delta\$G = 237 kJ\cdot mol&\#8315;&\#185;.\normalsize

\begin{equation}
E_{\mathrm{tn}} = \frac{\Delta H}{nF}
\end{equation}
\noindent\small The thermoneutral potential, 1481215 \$\mu\$V. It exceeds the reversible potential because the entropy term is negative for this reaction; the difference is the heat the surroundings must supply.\normalsize

\begin{equation}
C_{\mathrm{be}} = 4195\ \mathrm{mg}\cdot\mathrm{L}^{-1}
\end{equation}
\noindent\small The break-even organic load, computed rather than asserted. Below it the process consumes more energy than it recovers.\normalsize

\begin{equation}
T = \frac{1}{60}\ \mathrm{s}
\end{equation}
\noindent\small The real-time deadline, stated before any measurement is taken rather than chosen after one. The criterion reports the worst observed step and the miss count when it is run; those are properties of the machine it runs on and are deliberately not printed here.\normalsize

\begin{equation}
t_{\max} - t_{\min} \leq \frac{T}{2}
\end{equation}
\noindent\small The jitter bound. The maximum is the measure and the mean is not: a system that misses one deadline in a thousand has missed a deadline.\normalsize

\begin{equation}
C(q) \in \Theta(2^{q}), \qquad C(q) \leq T
\end{equation}
\noindent\small The cost of a full step --- a Hadamard layer, a CNOT ladder and a probability read --- is linear in the number of amplitudes and so doubles with every qubit. The criterion reports the widest register that fits the frame on the machine it runs on; a deadline met at qubits is met for every narrower one, and that implication is the part which does not depend on the machine.\normalsize


\section*{What is not established}
\paragraph{\texttt{lwe_hardness}} a universal negative over all polynomial-time algorithms; no finite computation decides it What is decided instead: kyber\_security seals the FIPS 203 parameter sizes of the shipped implementation

\paragraph{\texttt{no_prior_art_is_undecidable}} absence of prior art is a universal negative over all published and unpublished work; no finite search decides it, and a search returning nothing establishes only that it returned nothing What is decided instead: prior-art.json records what each contribution is, which files carry it, what was searched and when, and the dated citable identifier under which it was published — publication defeats a later claim of invention without requiring a proof of novelty

\paragraph{\texttt{awareness_is_undecided}} whether any physical process is conscious is unsettled and the major theories disagree about what would count as evidence; there is no predicate to run, so nothing here can seal it What is decided instead: consciousness-criterion runs five necessary conditions and reports how many a subject fails to be refuted by; it declines to claim awareness at all

\paragraph{} NO PHYSICAL EXPERIMENT HAS BEEN RUN. No coil was built, no field was measured, no resonance was observed, and nothing in this repository touches an instrument. Every condition here is about arithmetic and about code that checks arithmetic. The empirical claim is not made.

\section*{Prior art}
Every contribution names the domains where its prior art would live; no entry is recorded as novel,
because novelty is a universal negative that no finite search decides. It rests on the axiom
\texttt{no\_prior\_art\_is\_undecidable}.
\begin{itemize}
\item \texttt{ratchet} --- known-art-in-part. Domains: continuous integration quality gates; technical debt management; software process metrics. Cited: \texttt{10.1145/3493244.3493271}.
\item \texttt{measure-fingerprinting} --- known-art-in-part. Domains: software measurement theory; construct validity of metrics; build reproducibility. Cited: \texttt{10.1145/3210459.3210461}.
\item \texttt{criterion-modules} --- known-art-in-part. Domains: consciousness science (IIT, GWT, higher-order theories); real-time systems; operating system design; release engineering. Cited: \texttt{10.1186/1471-2202-5-42}, \texttt{10.1016/S0010-0277(00)00123-2}, \texttt{10.1016/j.tics.2011.05.009}, \texttt{10.1145/321738.321743}.
\item \texttt{claim-ledger-and-witness} --- known-art-in-part. Domains: assurance and safety cases; claim-to-evidence traceability; documentation testing. Cited: \texttt{10.1016/j.ress.2022.108933}.
\item \texttt{law-mutation-harness} --- known-art. Domains: mutation testing; test adequacy criteria. Cited: \texttt{10.1109/C-M.1978.218136}, \texttt{10.1109/TSE.2010.62}.
\item \texttt{side-effect-measurement} --- known-art-in-part. Domains: JavaScript module semantics; dead code elimination and tree shaking; bundler design; dynamic program analysis. Cited: \texttt{10.1109/saner.2018.8330226}, \texttt{10.1145/3517745.3561427}.
\item \texttt{lean-arbiter} --- known-art-in-part. Domains: interactive theorem proving; proof auditing and trusted computing base; proof-carrying artifacts. Cited: \texttt{10.1145/3372885.3373824}.
\item \texttt{digit-space-theorems} --- known-art. Domains: elementary number theory; formalisation of arithmetic in proof assistants. No cited work carries a DOI.
\item \texttt{generated-paper} --- known-art-in-part. Domains: reproducible research; literate programming; executable publications; MathML and technical typesetting. Cited: \texttt{10.1093/comjnl/27.2.97}, \texttt{10.1016/j.procs.2012.04.047}.
\item \texttt{ml-kem-768} --- known-art. Domains: post-quantum cryptography; cryptographic standards conformance; side-channel resistance. Cited: \texttt{10.6028/NIST.FIPS.203}.
\item \texttt{axiom-index} --- known-art-in-part. Domains: interactive theorem proving; trusted computing base and proof checking; extensionality and quotient types in type theory; proof by reflection and decision procedures; axiom minimisation in formal developments. Cited: \texttt{10.1007/978-1-4471-0963-1\_5}, \texttt{10.1145/2914770.2837638}, \texttt{10.1007/978-3-642-79361-5\_3}, \texttt{10.1109/cac48633.2019.8996365}.
\end{itemize}

\section*{Every Lean statement}
All 63 statements in the \texttt{lean/} sources. \emph{Proven} means the kernel accepted the file,
the proof contains no \texttt{sorry}, and \texttt{\#print axioms} reports a dependency set within
$\{$\texttt{propext}, \texttt{Quot.sound}$\}$. 39 of them meet it.
\begin{itemize}\small
\item \texttt{dr\_is\_drTS\_above\_zero} (DigitSpace.lean) --- $\forall$ n $\in$ List.range 60, n $\neq$ 0 $\to$ dr n = drTS n\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{dr\_and\_drTS\_differ\_at\_zero} (DigitSpace.lean) --- dr 0 = 1 $\land$ drTS 0 = 9\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{dr\_idempotent} (DigitSpace.lean) --- $\forall$ n $\in$ List.range 60, dr (dr n) = dr n\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{dr\_invariant\_under\_nine} (DigitSpace.lean) --- $\forall$ n $\in$ List.range 50, dr (n + 1) = dr (n + 10)\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{doubling\_stays\_in\_orbit} (DigitSpace.lean) --- $\forall$ d $\in$ orbit, dbl d $\in$ orbit\\
\textbf{proven by the kernel}, resting on \texttt{propext}, \texttt{Quot.sound}
\item \texttt{orbit\_closes\_after\_six} (DigitSpace.lean) --- $\forall$ d $\in$ orbit, dbl (dbl (dbl (dbl (dbl (dbl d))))) = d\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{orbit\_never\_repeats\_a\_step} (DigitSpace.lean) --- $\forall$ d $\in$ orbit, dbl d $\neq$ d\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{through\_void\_is\_an\_involution} (DigitSpace.lean) --- $\forall$ d $\in$ digits, tv (tv d) = d\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{through\_void\_fixes\_only\_zero\_and\_five} (DigitSpace.lean) --- $\forall$ d $\in$ digits, tv d = d $\leftrightarrow$ (d = 0 $\lor$ d = 5)\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{orbit\_and\_axis\_are\_disjoint} (DigitSpace.lean) --- $\forall$ d $\in$ orbit, d $\notin$ axis\\
\textbf{proven by the kernel}, resting on \texttt{propext}, \texttt{Quot.sound}
\item \texttt{orbit\_and\_axis\_and\_void\_exhaust\_the\_digits} (DigitSpace.lean) --- $\forall$ d $\in$ digits, d = 0 $\lor$ d $\in$ orbit $\lor$ d $\in$ axis\\
\textbf{proven by the kernel}, resting on \texttt{propext}, \texttt{Quot.sound}
\item \texttt{agl\_has\_order\_54} (DigitSpace.lean) --- agl.length = 54 $\land$ agl.Nodup\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{doubling\_and\_mirror\_are\_affine} (DigitSpace.lean) --- affineTable 2 0 $\in$ agl $\land$ affineTable 8 1 $\in$ agl\\
\textbf{proven by the kernel}, resting on \texttt{propext}, \texttt{Quot.sound}
\item \texttt{mirror\_table\_is\_through\_void} (DigitSpace.lean) --- $\forall$ d $\in$ nonzero, (affineTable 8 1).getD (d \% 9) 0 = tv d \% 9\\
\textbf{proven by the kernel}, resting on \texttt{propext}
\item \texttt{mirror\_is\_affine\_only\_off\_the\_void} (DigitSpace.lean) --- tv 9 = 1 $\land$ tv 0 = 0 $\land$ (affineTable 8 1).getD 0 0 = 1\\
\textbf{proven by the kernel}, resting on \texttt{propext}
\item \texttt{generators\_apart\_give\_twelve} (DigitSpace.lean) --- 6 * 2 = 12 $\land$ 54 - 12 = 42\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{whole\_axis\_and\_root\_nine\_is\_exactly\_thirty\_six} (DigitSpace.lean) --- $\forall$ b $\in$ List.range 100, (((b * 3) \% 12 = 0 $\land$ (b * 6) \% 12 = 0 $\land$ (b * 9) \% 12 = 0) $\land$ b \% 9 = 0) $\leftrightarrow$ b \% 36 = 0\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{four\_three\_two\_is\_a\_multiple\_of\_thirty\_six\_but\_not\_the\_only\_one} (DigitSpace.lean) --- 432 \% 36 = 0 $\land$ 396 \% 36 = 0 $\land$ 468 \% 36 = 0 $\land$ 432 = 36 * 12\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{base\_frequency\_has\_digital\_root\_nine} (DigitSpace.lean) --- dr 432 = 9\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{every\_axis\_digit\_mirrors\_an\_orbit\_digit} (DigitSpace.lean) --- $\forall$ d $\in$ axis, tv d $\in$ orbit\\
\textbf{proven by the kernel}, resting on \texttt{propext}, \texttt{Quot.sound}
\item \texttt{the\_axis\_is\_mirrored\_onto\_exactly\_seven\_four\_one} (DigitSpace.lean) --- axis.map tv = [7, 4, 1] $\land$ [7, 4, 1].map tv = axis\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{exactly\_two\_digits\_are\_their\_own\_mirror} (DigitSpace.lean) --- (digits.filter (fun d => tv d == d)) = [0, 5]\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{the\_ten\_digits\_are\_two\_fixed\_points\_and\_four\_pairs} (DigitSpace.lean) --- (digits.filter (fun d => tv d == d)).length = 2 $\land$ (digits.filter (fun d => tv d != d)).length = 8 $\land$ 2 + 4 * 2 = 10\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{the\_four\_pairs\_are\_one\_nine\_two\_eight\_three\_seven\_four\_six} (DigitSpace.lean) --- tv 1 = 9 $\land$ tv 2 = 8 $\land$ tv 3 = 7 $\land$ tv 4 = 6\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{four\_three\_two\_has\_twenty\_divisors\_its\_neighbours\_eighteen} (DigitSpace.lean) --- divisorCount 432 = 20 $\land$ divisorCount 396 = 18 $\land$ divisorCount 468 = 18\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{all\_three\_are\_multiples\_of\_thirty\_six} (DigitSpace.lean) --- 432 \% 36 = 0 $\land$ 396 \% 36 = 0 $\land$ 468 \% 36 = 0\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{every\_mirror\_orbit\_sums\_to\_ten} (DigitSpace.lean) --- $\forall$ d $\in$ nonzero, d + tv d = 10\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{the\_void\_is\_the\_one\_orbit\_that\_does\_not} (DigitSpace.lean) --- 0 + tv 0 = 0\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{swap12\_is\_an\_involution} (DigitSpace.lean) --- $\forall$ d $\in$ digits, swap12 (swap12 d) = d\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{being\_an\_involution\_is\_not\_enough\_for\_harmony} (DigitSpace.lean) --- swap12 1 + 1 $\neq$ swap12 3 + 3\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{non\_fixed\_points\_come\_in\_pairs} (DigitSpace.lean) --- (digits.filter (fun d => tv d != d)).length \% 2 = 0 $\land$ (digits.filter (fun d => swap12 d != d)).length \% 2 = 0\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{the\_mirror\_is\_the\_involution\_with\_a\_constant\_orbit\_sum} (DigitSpace.lean) --- (nonzero.map (fun d => d + tv d)) = [10, 10, 10, 10, 10, 10, 10, 10, 10] $\land$ (nonzero.map (fun d => d + swap12 d)) $\neq$ [10, 10, 10, 10, 10, 10, 10, 10, 10]\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{doubling\_stays\_in\_orbit\_axiom\_free} (DigitSpace.lean) --- orbit.all (fun d => orbit.contains (dbl d)) = true\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{orbit\_and\_axis\_are\_disjoint\_axiom\_free} (DigitSpace.lean) --- orbit.all (fun d => !axis.contains d) = true\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{orbit\_axis\_void\_exhaust\_the\_digits\_axiom\_free} (DigitSpace.lean) --- digits.all (fun d => d == 0 || orbit.contains d || axis.contains d) = true\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{every\_axis\_digit\_mirrors\_an\_orbit\_digit\_axiom\_free} (DigitSpace.lean) --- axis.all (fun d => orbit.contains (tv d)) = true\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{mirror\_table\_is\_through\_void\_axiom\_free} (DigitSpace.lean) --- nonzero.all (fun d => (affineTable 8 1).getD (d \% 9) 0 == tv d \% 9) = true\\
\textbf{proven by the kernel}, resting on \texttt{propext}
\item \texttt{doubling\_and\_mirror\_are\_affine\_axiom\_free} (DigitSpace.lean) --- (agl.contains (affineTable 2 0) && agl.contains (affineTable 8 1)) = true\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{every\_mirror\_orbit\_sums\_to\_ten\_axiom\_free} (DigitSpace.lean) --- nonzero.all (fun d => d + tv d == 10) = true\\
\textbf{proven by the kernel}, resting on no axioms
\item \texttt{hadamard\_squared} (Quantum.lean) --- hadamard * hadamard = 1\\
\textbf{needs a library this repository cannot build here}
\item \texttt{hadamard\_unitary} (Quantum.lean) --- IsUnitary hadamard\\
\textbf{written down, closed with sorry}
\item \texttt{pauliX\_unitary} (Quantum.lean) --- IsUnitary pauliX\\
\textbf{needs a library this repository cannot build here}
\item \texttt{pauli\_anticomm} (Quantum.lean) --- pauliX * pauliY = -pauliY * pauliX\\
\textbf{needs a library this repository cannot build here}
\item \texttt{born\_rule\_sum} (Quantum.lean) --- ($\psi$ : QuantumState n) (h : IsNormalized $\psi$) : $\sum$ i, measurementProb $\psi$ i = 1\\
\textbf{needs a library this repository cannot build here}
\item \texttt{grover\_amplification} (Quantum.lean) --- ($\psi$ : QuantumState n) (marked : Fin n $\to$ Prop) (h : IsNormalized $\psi$) : $\exists$ $\psi$\_final, IsNormalized $\psi$\_final $\land$ $\forall$ i, marked i $\to$ measurementProb $\psi$\_final i $\geq$ 1 / n\\
\textbf{written down, closed with sorry}
\item \texttt{shor\_period\_finding} (Quantum.lean) --- (N n : $\mathbb{N}$) (hN : N > 1) (hn : n > 0) : $\exists$ r : $\mathbb{N}$, r > 0 $\land$ Nat.ModEq (a ^ r) 1 N\\
\textbf{written down, closed with sorry}
\item \texttt{repetition\_detects\_single\_error} (Quantum.lean) --- ($\psi$ : QuantumState 2) : $\exists$ syndrome : Fin 2 $\to$ Fin 2, syndrome uniquely\_identifies\_error\_location\\
\textbf{written down, closed with sorry}
\item \texttt{surface\_code\_correctability} (Quantum.lean) --- (error\_rate : $\mathbb{R}$) (h : error\_rate < surfaceCodeThreshold) : logical\_error\_rate $\leq$ O (error\_rate ^ 2)\\
\textbf{written down, closed with sorry}
\item \texttt{vqe\_convergence} (Quantum.lean) --- (H : Matrix (Fin n) (Fin n) $\mathbb{C}$) (E\_0 : $\mathbb{R}$) (h\_ground : is\_ground\_state\_energy H E\_0) : $\exists$ $\theta$\_opt, vqeObjective $\theta$\_opt H $\psi$ $\leq$ E\_0 + $\varepsilon$\\
\textbf{written down, closed with sorry}
\item \texttt{tensor\_preserves\_norm} (Quantum.lean) --- ($\psi$$_1$ : QuantumState n) ($\psi$$_2$ : QuantumState m) (h$_1$ : IsNormalized $\psi$$_1$) (h$_2$ : IsNormalized $\psi$$_2$) : IsNormalized (tensorProduct $\psi$$_1$ $\psi$$_2$)\\
\textbf{written down, closed with sorry}
\item \texttt{surface\_code\_fault\_tolerant} (Quantum.lean) --- IsFaultTolerant (fun n => 2 * n * Nat.log 2 n)\\
\textbf{written down, closed with sorry}
\item \texttt{kyber\_security} (Quantum.lean) --- $\exists$ (k : $\mathbb{N}$), 128 $\leq$ k $\land$ kyber\_security\_level = k\\
\textbf{needs a library this repository cannot build here}
\item \texttt{quantum\_system\_correct} (Quantum.lean) --- ($\forall$ U $\in$ [hadamard, pauliX, pauliY, pauliZ], IsUnitary U) $\land$ ($\forall$ $\psi$ : QuantumState n, IsNormalized $\psi$ $\to$ $\forall$ U $\in$ gate\_set, IsNormalized (apply\_gate U $\psi$)) $\land$ ($\forall$ $\psi$ : QuantumState (2^k), $\exists$ $\psi$\_corrected, detects\_and\_corrects\_errors $\psi$ $\psi$\_corrected) $\land$ ($\forall$ $\psi$$_1$ $\psi$$_2$, IsNormalized $\psi$$_1$ $\to$ IsNormalized $\psi$$_2$ $\to$ IsNormalized (tensorProduct $\psi$$_1$ $\psi$$_2$)) $\land$ ($\forall$ scheme $\in$ [kyber, sphincs\_plus], is\_post\_quantum\_secure scheme)\\
\textbf{written down, closed with sorry}
\item \texttt{grover\_amplification} (QuantumAlgorithms.lean) --- (n : $\mathbb{N}$) (k : $\mathbb{N}$) (marked : Fin n $\to$ Prop) (h\_marked : Finset.card (Finset.filter marked Finset.univ) = 1) : let m\\
\textbf{written down, closed with sorry}
\item \texttt{grover\_speedup} (QuantumAlgorithms.lean) --- (n : $\mathbb{N}$) (marked : Fin n $\to$ Prop) (h\_marked : Finset.card (Finset.filter marked Finset.univ) = 1) : let classical\_queries\\
\textbf{written down, closed with sorry}
\item \texttt{qft\_unitary} (QuantumAlgorithms.lean) --- (n : $\mathbb{N}$) : IsUnitary (qftMatrix n)\\
\textbf{written down, closed with sorry}
\item \texttt{qft\_inverse} (QuantumAlgorithms.lean) --- (n : $\mathbb{N}$) : qftMatrix n * (qftMatrix n)$^{H}$ = 1\\
\textbf{written down, closed with sorry}
\item \texttt{qft\_correctness} (QuantumAlgorithms.lean) --- (n : $\mathbb{N}$) (x : Fin (2^n)) : let input : QuantumState (2^n)\\
\textbf{written down, closed with sorry}
\item \texttt{phase\_estimation\_accuracy} (QuantumAlgorithms.lean) --- (U : Matrix (Fin n) (Fin n) $\mathbb{C}$) ($\psi$ : QuantumState n) (h\_unitary : IsUnitary U) (precision : $\mathbb{N}$) (h\_prec : precision $\geq$ 4) : $\exists$ ($\theta$ : $\mathbb{R}$), U.eigenvalue $\psi$ = Complex.exp (2 * $\pi$ * I * $\theta$) $\land$ let $\theta$\_est\\
\textbf{written down, closed with sorry}
\item \texttt{shor\_period\_finding} (QuantumAlgorithms.lean) --- (a N n : $\mathbb{N}$) (ha : 1 0 $\land$ r < N $\land$ a ^ r $\equiv$ 1 [MOD N] $\land$ period\_findable\_via\_qft r (modularExpSuperposition a N n)\\
\textbf{written down, closed with sorry}
\item \texttt{shor\_factorization} (QuantumAlgorithms.lean) --- (N : $\mathbb{N}$) (h\_composite : $\lnot$Nat.Prime N) : $\exists$ (p q : $\mathbb{N}$), N = p * q $\land$ p > 1 $\land$ q > 1\\
\textbf{written down, closed with sorry}
\item \texttt{vqe\_finds\_ground\_state} (QuantumAlgorithms.lean) --- (H : Matrix (Fin n) (Fin n) $\mathbb{C}$) (E\_0 : $\mathbb{R}$) (h\_ground : is\_ground\_state\_energy H E\_0) (h\_expressibility : ground\_state\_in\_ansatz\_space) : $\exists$ $\theta$\_opt : $\mathbb{R}$ $\to$ $\mathbb{R}$, vqeObjective H $\theta$\_opt = E\_0\\
\textbf{written down, closed with sorry}
\item \texttt{qaoa\_approximation} (QuantumAlgorithms.lean) --- (graph : SimpleGraph $\alpha$) (C : $\mathbb{R}$) (h\_maxcut : C = maximum\_cut\_value graph) (p : $\mathbb{N}$) (h\_p : p $\geq$ 1) : $\exists$ ($\theta$ : $\mathbb{R}$ $\to$ $\mathbb{R}$), qaoa\_expectation graph p $\theta$ $\geq$ 0.924 * C\\
\textbf{written down, closed with sorry}
\end{itemize}

\section*{What this work claims}
\begin{enumerate}\small
\item \textbf{Twelve measured surfaces whose ceilings only move down, failing on unrecorded improvement as well as on growth, with each deliberate raise recorded with its reason.} --- known-art-in-part.
\item \textbf{Each measure's implementation is fingerprinted from its syntax tree printed WITHOUT comments, so rewording a measure is free and changing what it counts fails until declared.} --- known-art-in-part.
\item \textbf{Predicates that decide whether a subject meets a named standard, each condition drawn from a theory with an operational commitment, each written to reject its own cheap pass, and each stating what would refute it. Six gate a release.} --- known-art-in-part.
\item \textbf{Every effect claim in the corpus bound to a predicate that runs or to a declared axiom, with a stated demarcation of what the binding does NOT establish; coverage checked both ways so a new claim fails for being unbound and a stale entry fails for having no claim.} --- known-art-in-part.
\item \textbf{Each test suite is required to FAIL when one line of the module under it is corrupted; a non-unique anchor is itself a failure, because a mutation landing on the wrong copy reports a false survivor.} --- known-art.
\item \textbf{The package's sideEffects list is MEASURED by importing every published entry point in its own process and observing new globals and surviving handles, rather than declared.} --- known-art-in-part.
\item \textbf{The .lean files are the source of truth; the kernel is run on the files that import nothing; TypeScript's ledger fails when it presents as proven anything the files close with sorry, and the generated status ships with the package.} --- known-art-in-part.
\item \textbf{The doubling orbit closing after six, the through-void involution and its two fixed points, and the orbit/axis/void partition, machine-checked by the Lean kernel with no imports.} --- known-art.
\item \textbf{A publication artifact in MathML and LaTeX from one generator, every figure read from the source at generation time, refusing to print any verdict it measured, and failing on a byte of drift.} --- known-art-in-part.
\item \textbf{An ML-KEM-768 implementation checked against NIST ACVP vectors and pq-crystals KAT cases.} --- known-art.
\item \textbf{An index that separates two things usually reported as one: assumptions about the SUBJECT, which no finite computation decides and which this repository declares in ASSUMPTIONS, from axioms of the PROOF SYSTEM, which a theorem inherits through the tactics that close it and which say nothing about the subject. Each kernel-proven theorem records its own dependency set as the evidence for the word proven, and the corpus reports how many rest on nothing at all.} --- known-art-in-part.
\end{enumerate}
\noindent\textbf{None of these is claimed to be novel.} Novelty is a universal negative and no finite search decides it.
What is decided is priority: a dated deposit at \texttt{doi:10.5281/zenodo.22178675}, which defeats a later claim of invention.

\section*{References}
\begin{enumerate}\small
\item Technical Debt Guild. \texttt{doi:10.1145/3493244.3493271}
\item Construct Validity in Software Engineering Research and Software Metrics. \texttt{doi:10.1145/3210459.3210461}
\item An information integration theory of consciousness. \texttt{doi:10.1186/1471-2202-5-42}
\item Towards a cognitive neuroscience of consciousness: basic evidence and a workspace framework. \texttt{doi:10.1016/S0010-0277(00)00123-2}
\item Empirical support for higher-order theories of conscious awareness. \texttt{doi:10.1016/j.tics.2011.05.009}
\item Scheduling Algorithms for Multiprogramming in a Hard-Real-Time Environment. \texttt{doi:10.1145/321738.321743}
\item Graphical safety assurance case using Goal Structuring Notation (GSN) — challenges, opportunities and a framework for autonomous trains. \texttt{doi:10.1016/j.ress.2022.108933}
\item Hints on Test Data Selection: Help for the Practicing Programmer. \texttt{doi:10.1109/C-M.1978.218136}
\item An Analysis and Survey of the Development of Mutation Testing. \texttt{doi:10.1109/TSE.2010.62}
\item An extensible approach for taming the challenges of JavaScript dead code elimination. \texttt{doi:10.1109/saner.2018.8330226}
\item Muzeel. \texttt{doi:10.1145/3517745.3561427}
\item The lean mathematical library. \texttt{doi:10.1145/3372885.3373824}
\item Literate Programming. \texttt{doi:10.1093/comjnl/27.2.97}
\item Literate Program Execution for Reproducible Research and Executable Papers. \texttt{doi:10.1016/j.procs.2012.04.047}
\item Module-lattice-based key-encapsulation mechanism standard. \texttt{doi:10.6028/NIST.FIPS.203}
\item Extensionality and quotient types. \texttt{doi:10.1007/978-1-4471-0963-1\_5}
\item Type theory in type theory using quotient inductive types. \texttt{doi:10.1145/2914770.2837638}
\item Using Reflection to Explain and Enhance Type Theory. \texttt{doi:10.1007/978-3-642-79361-5\_3}
\item A Formal Proof in Coq of Cantor-Bernstein-Schroeder’s Theorem without axiom of choice. \texttt{doi:10.1109/cac48633.2019.8996365}
\end{enumerate}
\noindent Archive: \texttt{doi:10.5281/zenodo.22178675}. Source: \texttt{https://github.com/ceccec/zeropoint-node}.
Package: \texttt{https://www.npmjs.com/package/zeropoint-node}. Author: Tsvetan Rouschev, \texttt{https://orcid.org/0009-0000-7312-9778}.

\section*{Reproduction}
\begin{verbatim}
npm install
npm run paper          # regenerate both artifacts
npm run paper:check    # fail if either has drifted
npm run lean:check     # what the Lean kernel accepts
npm run criteria:check # the six predicates
npm run priorart:resolve # re-resolve every DOI (network)
\end{verbatim}
\end{document}
