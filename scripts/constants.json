{
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.1.2.4.8.7.5.1.ts::VORTEX_AXIS": {
    "count": 2,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.1.2.4.8.7.5.ts::VORTEX_ORBIT": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.block.chain.ts::VORTEX_SEQUENCE": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.color.ts::VORTEX_AXIS": {
    "count": 2,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.color.ts::VORTEX_ORBIT": {
    "count": 2,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.consciousness.journey.3d.ts::VORTEX_AXIS": {
    "count": 2,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.electric.flow.ts::VORTEX_AXIS": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.empty.streams.ts::VORTEX_ORBIT": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.matrix.ts::VORTEX_AXIS": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.multidimensional.consciousness.3d.ts::VORTEX_AXIS": {
    "count": 2,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.multidimensional.consciousness.3d.ts::VORTEX_ORBIT": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.rodin.cmyk.ts::VORTEX_AXIS": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.rodin.cmyk.ts::VORTEX_ORBIT": {
    "count": 2,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.rodin.coil.harmonic.ts::VORTEX_ORBIT": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.sacred.geometry.ts::VORTEX_AXIS": {
    "count": 4,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.sacred.geometry.ts::VORTEX_ORBIT": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.triple.merkaba.3d.ts::VORTEX_AXIS": {
    "count": 4,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.triple.merkaba.3d.ts::VORTEX_ORBIT": {
    "count": 2,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.triple.merkaba.ts::VORTEX_AXIS": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.triple.merkaba.ts::VORTEX_ORBIT": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.vbm.animation.ts::VORTEX_AXIS": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/0/3/6/9/1/2/4/8/7/5/1/a432.vortex.cmyk.matrix.ts::VORTEX_ORBIT": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/advanced-vbm.ts::VORTEX_AXIS": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/security/quantum-fold-cipher.ts::VORTEX_AXIS": {
    "count": 2,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/security/quantum-threat-landscape.ts::VORTEX_AXIS": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/security/quantum-threat-landscape.ts::VORTEX_ORBIT": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/vbm-math.ts::VORTEX_AXIS": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  },
  "src/verification/lean-bridge.ts::VORTEX_ORBIT": {
    "count": 1,
    "why": "kept: this site's literal carries a narrower type than the kernel constant does through an import. Binding it — by spread or directly — widens a '3 | 6 | 9' to 'number' at the call site and adds type errors. The copy is real entropy; closing it needs the consuming signatures widened or the kernel constant re-typed, which changes a contract rather than a name."
  }
}
