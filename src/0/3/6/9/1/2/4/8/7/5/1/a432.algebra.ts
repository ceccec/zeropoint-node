/**
 * a432.algebra — local re-export of src/0 algebra (no Math.*).
 * Import from here inside the digit stream so strip-types paths stay local.
 */

export {
  PI,
  TAU,
  E,
  imul,
  abs,
  sign,
  trunc,
  floor,
  ceil,
  round,
  min,
  max,
  sin,
  cos,
  tan,
  sqrt,
  hypot,
  pow,
  exp,
  log,
  log2,
  unitFromSeed,
  indexFromSeed,
} from '../../../../../../../../../../algebra.ts'
