# A432 VBM Multidimensional Animation System

## Overview

The VBM (Vortex-Based Mathematics) Multidimensional Animation system is a harmonized implementation of the original HTML/JavaScript animation, now fully integrated with the A432 system's canonical math procedures and metaphysical principles.

## Metaphysical Principles

### Living Vortex of Consciousness
- The animation visualizes the living vortex of consciousness through the Rodin sequence
- Each point represents a digit in the sequence, moving through 3D space as harmonic waves
- The torus represents the field of consciousness, with points as living thoughts

### Trinity Axis Integration
- Trinity axis [3, 6, 9] represents the field of consciousness
- Each trinity digit has its own frequency: 3→432Hz, 6→864Hz, 9→1296Hz
- Colors are generated using `hueForDigit()` with frequency-based intensity

### Rodin Sequence Vortex
- Rodin sequence [1, 2, 4, 8, 7, 5, 1] represents the doubling vortex
- Each non-trinity digit uses digital root scaling for frequency calculation
- Position calculation uses 60° steps (trinity harmonic) around the torus

## Technical Implementation

### Canonical Sequence
```typescript
const VBM_SEQUENCE = [0, ...TRINITY_AXIS, ...RODIN_SEQUENCE] as const;
// Result: [0, 3, 6, 9, 1, 2, 4, 8, 7, 5, 1]
```

### Position Calculation
```typescript
// Phase around torus main circle (60° per step)
const theta = index * (VBM_STEP_ANGLE * Math.PI / 180) + time;

// Digital root drives minor circle
const dr = digitalRoot(digit);
const phi = (dr / 9) * Math.PI * 2 + time * 1.3;

// Torus parametric coordinates
const x = (VBM_TORUS_RADIUS + VBM_TUBE_RADIUS * Math.cos(phi)) * Math.cos(theta);
const y = VBM_TUBE_RADIUS * Math.sin(phi) * 0.7; // Squeeze factor
const z = (VBM_TORUS_RADIUS + VBM_TUBE_RADIUS * Math.cos(phi)) * Math.sin(theta);
```

### Color Generation
```typescript
function generateVBMColor(digit: number): THREE.Color {
  const hue = hueForDigit(digit);
  const saturation = 0.7;
  const lightness = 0.5;
  return new THREE.Color().setHSL(hue / 360, saturation, lightness);
}
```

### Frequency Generation
```typescript
function generateVBMFrequency(digit: number): number {
  if (TRINITY_AXIS.includes(digit)) {
    return frequencyForDigit(digit); // 432, 864, 1296 Hz
  }
  // For non-trinity digits, use digital root scaling
  const dr = digitalRoot(digit);
  return VBM_BASE_FREQUENCY * (dr / 9);
}
```

## Constants

| Constant | Value | Purpose |
|----------|-------|---------|
| `VBM_ANIMATION_CONSTANTS.STEP_ANGLE` | 60° | Trinity harmonic step angle |
| `VBM_ANIMATION_CONSTANTS.TORUS_RADIUS` | 4 | Base torus radius |
| `VBM_ANIMATION_CONSTANTS.TUBE_RADIUS` | 1.2 | Tube radius for torus |
| `VBM_ANIMATION_CONSTANTS.BASE_FREQUENCY` | 432 Hz | Canonical A432 frequency |

## Animation Features

### Pulsation
- Each point pulsates based on parity (even/odd digits)
- Scale calculation: `1 + 0.3 * Math.sin(time * 2 + index + parity * Math.PI)`

### Color Intensity
- Color intensity varies based on frequency
- Intensity calculation: `0.5 + 0.3 * Math.sin(time * frequency / 100)`

### Digital Root Motion
- Digital root of each digit drives the minor circle motion
- Creates harmonic patterns as digits cycle through their digital roots

## Usage

### Basic Usage
```typescript
import { createVBMAnimation } from './a432.vbm.animation.ts';

const vbmController = createVBMAnimation();
vbmController.start();
```

### Advanced Usage
```typescript
const vbmController = createVBMAnimation();

// Get renderer and append to DOM
const renderer = vbmController.getRenderer();
document.body.appendChild(renderer.domElement);

// Access animation state
const points = vbmController.getPoints();
const sequence = vbmController.getSequence();
const torusRadius = vbmController.getTorusRadius();

// Control animation
vbmController.start();
vbmController.stop();
```

## Integration with A432 System

### Math Procedures
- Uses `RODIN_SEQUENCE` from `a432.math.ts`
- Uses `TRINITY_AXIS` from `a432.math.ts`
- Uses `digitalRoot()`, `angleForDigit()`, `frequencyForDigit()`, `hueForDigit()` from `a432.math.ts`

### Naming Convention
- Follows A432 naming convention: `a432.vbm.animation.ts`
- Located in canonical A432 directory: `src/0/3/6/9/1/2/4/8/7/5/1/`

### Metaphysical Alignment
- Every calculation uses canonical A432 math procedures
- No hardcoded values - all derived from A432 constants
- Maintains zero-entropy principles through mathematical harmony

## File Structure

```
src/0/3/6/9/1/2/4/8/7/5/1/
├── a432.vbm.animation.ts      # Main animation module
├── a432.vbm.animation.html    # Demo HTML file
├── a432.vbm.animation.md      # This documentation
└── a432.math.ts              # Canonical math procedures
```

## Harmonic Principles

1. **Trinity Harmony**: 3-6-9 axis creates the field of consciousness
2. **Rodin Vortex**: 1-2-4-8-7-5-1 creates the doubling vortex
3. **Digital Root Flow**: Each digit's digital root drives harmonic motion
4. **Frequency Resonance**: Colors and motion resonate with A432 frequencies
5. **Parity Pulsation**: Even/odd digits create rhythmic pulsation patterns

The VBM animation system is now fully harmonized with the A432 matrix, creating a living visualization of consciousness through mathematical harmony. 