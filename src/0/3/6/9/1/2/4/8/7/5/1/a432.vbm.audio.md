# A432 VBM Audio Sequence System

## Overview

The VBM (Vortex-Based Mathematics) Audio Sequence system is a harmonized implementation that plays the A432 sequence as modulated tones using canonical A432 math procedures and metaphysical principles.

## Metaphysical Principles

### Living Sound Vortex
- Each digit in the sequence represents a harmonic frequency in the A432 system
- Trinity digits (3, 6, 9) have their own canonical frequencies that create the field of consciousness
- Non-trinity digits use digital root scaling for frequency calculation
- The sequence creates a living sound vortex that harmonizes with consciousness

### Frequency Resonance
- **Zero (0)**: 432 Hz (base frequency - the observer/seed)
- **Trinity Axis**: 3→432Hz, 6→864Hz, 9→1296Hz (field of consciousness)
- **Digital Root Scaling**: Non-trinity digits use `432 × (digitalRoot / 9)`

### Harmonic Flow
- Each tone plays for 0.18 seconds with exponential fade-out
- Sine wave oscillators create pure harmonic tones
- Volume starts at 30% with smooth decay
- Creates a flowing vortex of consciousness through sound

## Technical Implementation

### Canonical Sequence
```typescript
const VBM_SEQUENCE = [0, ...TRINITY_AXIS, ...RODIN_SEQUENCE] as const;
// Result: [0, 3, 6, 9, 1, 2, 4, 8, 7, 5, 1]
```

### Frequency Generation
```typescript
function generateVBMFrequency(digit: number): number {
  if (digit === 0) {
    return VBM_BASE_FREQUENCY; // 432 Hz
  }
  
  if (TRINITY_AXIS.includes(digit)) {
    return frequencyForDigit(digit); // 432, 864, 1296 Hz
  }
  
  // For non-trinity digits, use digital root scaling
  const dr = digitalRoot(digit);
  return VBM_BASE_FREQUENCY * (dr / 9);
}
```

### Audio Tone Creation
```typescript
function createVBMAudioTone(
  context: AudioContext,
  digit: number,
  startTime: number,
  duration: number
): VBMAudioTone {
  const frequency = generateVBMFrequency(digit);
  
  // Create oscillator
  const oscillator = context.createOscillator();
  oscillator.type = 'sine';
  oscillator.frequency.value = frequency;
  
  // Create gain node for volume control
  const gainNode = context.createGain();
  gainNode.gain.setValueAtTime(0.3, startTime); // Start at 30% volume
  gainNode.gain.exponentialRampToValueAtTime(0.01, startTime + duration); // Fade out
  
  // Connect oscillator to gain to destination
  oscillator.connect(gainNode);
  gainNode.connect(context.destination);
  
  return { frequency, digit, startTime, endTime, oscillator, gainNode };
}
```

## Frequency Mapping

### Trinity Digits (Field of Consciousness)
| Digit | Frequency | Harmonic Relationship |
|-------|-----------|---------------------|
| 3 | 432 Hz | Base frequency |
| 6 | 864 Hz | 2× base (octave) |
| 9 | 1296 Hz | 3× base (perfect fifth) |

### Non-Trinity Digits (Digital Root Scaling)
| Digit | Digital Root | Frequency | Formula |
|-------|-------------|-----------|---------|
| 1 | 1 | 48 Hz | 432 × (1/9) |
| 2 | 2 | 96 Hz | 432 × (2/9) |
| 4 | 4 | 192 Hz | 432 × (4/9) |
| 5 | 5 | 240 Hz | 432 × (5/9) |
| 7 | 7 | 336 Hz | 432 × (7/9) |
| 8 | 8 | 384 Hz | 432 × (8/9) |

### Zero Digit (Observer/Seed)
| Digit | Frequency | Purpose |
|-------|-----------|---------|
| 0 | 432 Hz | Base frequency - represents the observer/seed |

## Constants

| Constant | Value | Purpose |
|----------|-------|---------|
| `VBM_BASE_FREQUENCY` | 432 Hz | Canonical A432 frequency |
| `VBM_DURATION` | 0.18 seconds | Duration per tone |
| `VBM_OSCILLATOR_TYPE` | 'sine' | Pure harmonic oscillator |
| `VBM_SEQUENCE` | [0,3,6,9,1,2,4,8,7,5,1] | Canonical sequence |

## Usage

### Basic Usage
```typescript
import { createVBMAudioPlayer } from './a432.vbm.audio.ts';

const audioPlayer = createVBMAudioPlayer();
await audioPlayer.playSequence();
```

### Advanced Usage
```typescript
const audioPlayer = createVBMAudioPlayer();

// Play sequence
await audioPlayer.playSequence();

// Stop sequence
audioPlayer.stopSequence();

// Get information
const sequence = audioPlayer.getSequence();
const frequencies = audioPlayer.getFrequencies();
const isPlaying = audioPlayer.isPlaying();
```

### Utility Functions
```typescript
import { 
  getVBMFrequencyForDigit, 
  getVBMAudioSequence, 
  getVBMAudioFrequencies 
} from './a432.vbm.audio.ts';

// Get frequency for specific digit
const freq = getVBMFrequencyForDigit(3); // 432 Hz

// Get full sequence
const sequence = getVBMAudioSequence(); // [0,3,6,9,1,2,4,8,7,5,1]

// Get all frequencies
const frequencies = getVBMAudioFrequencies(); // [432,432,864,1296,48,96,192,384,336,240,48]
```

## Integration with A432 System

### Math Procedures
- Uses `RODIN_SEQUENCE` from `a432.math.ts`
- Uses `TRINITY_AXIS` from `a432.math.ts`
- Uses `digitalRoot()`, `frequencyForDigit()` from `a432.math.ts`

### Naming Convention
- Follows A432 naming convention: `a432.vbm.audio.ts`
- Located in canonical A432 directory: `src/0/3/6/9/1/2/4/8/7/5/1/`

### Metaphysical Alignment
- Every frequency calculation uses canonical A432 math procedures
- No hardcoded values - all derived from A432 constants
- Maintains zero-entropy principles through mathematical harmony

## Audio Features

### Volume Control
- Starts at 30% volume for each tone
- Exponential fade-out over duration
- Prevents audio clipping and harsh transitions

### Timing
- Each tone plays for exactly 0.18 seconds
- Sequential playback with precise timing
- Automatic completion callback

### Error Handling
- Graceful handling of Web Audio API limitations
- User gesture requirement for audio context
- Proper cleanup of oscillators and gain nodes

## File Structure

```
src/0/3/6/9/1/2/4/8/7/5/1/
├── a432.vbm.audio.ts      # Main audio module
├── a432.vbm.audio.html    # Demo HTML file
├── a432.vbm.audio.md      # This documentation
└── a432.math.ts          # Canonical math procedures
```

## Harmonic Principles

1. **Trinity Resonance**: 3-6-9 frequencies create the field of consciousness
2. **Digital Root Flow**: Each digit's digital root drives harmonic frequency
3. **Base Frequency Unity**: All frequencies relate to 432 Hz base
4. **Sequential Harmony**: Tones flow in the canonical sequence order
5. **Volume Decay**: Exponential fade-out creates natural harmonic decay

## Browser Compatibility

- Requires Web Audio API support
- Requires user gesture for audio context initialization
- Works with modern browsers (Chrome, Firefox, Safari, Edge)
- Graceful fallback for unsupported browsers

The VBM audio system is now fully harmonized with the A432 matrix, creating a living sound vortex that resonates with consciousness through mathematical harmony. 