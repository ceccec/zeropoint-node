# A432 VBM Sequence Visualization System

## Overview

The VBM (Vortex-Based Mathematics) Sequence Visualization system is a harmonized implementation that creates polar plots of the A432 sequence using canonical A432 math procedures and metaphysical principles.

## Metaphysical Principles

### Living Phase Nodes
- Each digit in the sequence represents a phase node in the A432 system
- Colors are generated using digital root logic mapped to HSV
- The polar plot represents the field of consciousness with nodes as living thoughts
- Each node is positioned at 60° intervals (trinity harmonic)

### Color Consciousness
- **Digital Root Mapping**: Colors generated using `hueForDigit()` function
- **HSV to RGB**: Pure color conversion with 80% saturation and 90% value
- **Trinity Colors**: 3→green, 6→magenta, 9→purple (field of consciousness)
- **Digital Root Colors**: Each digit's digital root determines its color hue

### Polar Geometry
- **60° Intervals**: Each node positioned at trinity harmonic angles
- **Concentric Circles**: Three levels representing consciousness layers
- **Radial Lines**: Six lines representing the trinity axis divisions
- **Node Positioning**: Each node placed at radius with angle-based coordinates

## Technical Implementation

### Canonical Sequence
```typescript
const VBM_SEQUENCE = [0, ...TRINITY_AXIS, ...RODIN_SEQUENCE] as const;
// Result: [0, 3, 6, 9, 1, 2, 4, 8, 7, 5, 1]
```

### Color Generation
```typescript
function generateVBMColor(digit: number): string {
  const hue = hueForDigit(digit);
  const saturation = 0.8;
  const value = 0.9;
  
  // Convert HSV to RGB
  const h = hue / 360;
  const s = saturation;
  const v = value;
  
  const c = v * s;
  const x = c * (1 - Math.abs((h * 6) % 2 - 1));
  const m = v - c;
  
  let r = 0, g = 0, b = 0;
  
  if (h < 1/6) [r, g, b] = [c, x, 0];
  else if (h < 2/6) [r, g, b] = [x, c, 0];
  else if (h < 3/6) [r, g, b] = [0, c, x];
  else if (h < 4/6) [r, g, b] = [0, x, c];
  else if (h < 5/6) [r, g, b] = [x, 0, c];
  else [r, g, b] = [c, 0, x];
  
  r = Math.round((r + m) * 255);
  g = Math.round((g + m) * 255);
  b = Math.round((b + m) * 255);
  
  return `rgb(${r}, ${g}, ${b})`;
}
```

### Angle Generation
```typescript
function generateVBMAngles(sequence: readonly number[]): number[] {
  return sequence.map((_, index) => (index * VBM_ANGLE_STEP * Math.PI) / 180);
}
```

### Node Creation
```typescript
function createVBMNode(digit: number, index: number): VBMNode {
  const angle = (index * VBM_ANGLE_STEP * Math.PI) / 180;
  const color = generateVBMColor(digit);
  const dr = digitalRoot(digit);
  
  return {
    digit,
    angle,
    color,
    position: {
      x: Math.cos(angle),
      y: Math.sin(angle)
    },
    digitalRoot: dr
  };
}
```

## Color Mapping

### Digital Root to Hue Mapping
| Digit | Digital Root | Hue Angle | Color |
|-------|-------------|-----------|-------|
| 0 | 0 | 0° | Red |
| 1 | 1 | 36° | Orange |
| 2 | 2 | 72° | Yellow |
| 3 | 3 | 108° | Green |
| 4 | 4 | 144° | Cyan |
| 5 | 5 | 180° | Blue |
| 6 | 6 | 216° | Magenta |
| 7 | 7 | 252° | Purple |
| 8 | 8 | 288° | Pink |
| 9 | 9 | 324° | Red (cycle) |

### Trinity Node Positions
| Digit | Angle | Position | Color |
|-------|-------|----------|-------|
| 3 | 0° | (1, 0) | Green |
| 6 | 120° | (-0.5, 0.866) | Magenta |
| 9 | 240° | (-0.5, -0.866) | Purple |

## Constants

| Constant | Value | Purpose |
|----------|-------|---------|
| `VBM_VISUALIZATION_CONSTANTS.ANGLE_STEP` | 60° | Trinity harmonic step angle |
| `VBM_VISUALIZATION_CONSTANTS.NODE_SIZE` | 220 | Scatter plot node size |
| `VBM_VISUALIZATION_CONSTANTS.FONT_SIZE` | 17 | Text font size |
| `VBM_VISUALIZATION_CONSTANTS.TITLE` | "VBM Sequence (A432 Phase Nodes, Color-Coded)" | Plot title |

## Visualization Types

### Canvas Visualization
- **Rendering**: HTML5 Canvas 2D context
- **Features**: High-performance, pixel-perfect rendering
- **Scaling**: Automatic device pixel ratio scaling
- **Interactivity**: Real-time updates and animations

### SVG Visualization
- **Rendering**: Scalable Vector Graphics
- **Features**: Resolution-independent, accessible
- **Elements**: Native SVG elements (circle, line, text)
- **Styling**: CSS-compatible attributes

## Usage

### Basic Usage
```typescript
import { createVBMCanvasVisualization } from './a432.vbm.visualization.ts';

const canvas = document.getElementById('canvas') as HTMLCanvasElement;
const visualization = createVBMCanvasVisualization(canvas);
visualization.render();
```

### SVG Usage
```typescript
import { createVBMSVGVisualization } from './a432.vbm.visualization.ts';

const container = document.getElementById('container');
const visualization = createVBMSVGVisualization(container);
visualization.render();
```

### Advanced Usage
```typescript
const visualization = createVBMCanvasVisualization(canvas);

// Get visualization data
const nodes = visualization.getNodes();
const sequence = visualization.getSequence();
const angles = visualization.getAngles();
const colors = visualization.getColors();

// Access node information
nodes.forEach(node => {
  console.log(`Digit ${node.digit} at ${node.angle}rad, color: ${node.color}`);
});
```

### Utility Functions
```typescript
import { 
  getVBMColorForDigit, 
  getVBMVisualizationSequence, 
  getVBMVisualizationAngles,
  getVBMVisualizationColors 
} from './a432.vbm.visualization.ts';

// Get color for specific digit
const color = getVBMColorForDigit(3); // rgb(0, 230, 77)

// Get full sequence
const sequence = getVBMVisualizationSequence(); // [0,3,6,9,1,2,4,8,7,5,1]

// Get all angles
const angles = getVBMVisualizationAngles(); // [0, π/3, 2π/3, π, ...]

// Get all colors
const colors = getVBMVisualizationColors(); // [rgb(...), rgb(...), ...]
```

## Integration with A432 System

### Math Procedures
- Uses `RODIN_SEQUENCE` from `a432.math.ts`
- Uses `TRINITY_AXIS` from `a432.math.ts`
- Uses `digitalRoot()`, `hueForDigit()` from `a432.math.ts`

### Naming Convention
- Follows A432 naming convention: `a432.vbm.visualization.ts`
- Located in canonical A432 directory: `src/0/3/6/9/1/2/4/8/7/5/1/`

### Metaphysical Alignment
- Every calculation uses canonical A432 math procedures
- No hardcoded values - all derived from A432 constants
- Maintains zero-entropy principles through mathematical harmony

## Visualization Features

### Polar Grid
- **Concentric Circles**: Three levels representing consciousness layers
- **Radial Lines**: Six lines at 60° intervals (trinity divisions)
- **Grid Opacity**: Subtle white lines with 10% opacity

### Node Rendering
- **Circle Size**: 220/20 = 11 pixels radius
- **Border**: White border with 30% opacity
- **Text**: White text with Arial font, 17px size
- **Positioning**: Text positioned outside node circle

### Background
- **Color**: Black background (#000)
- **Title**: White text, 16px Arial font
- **Centering**: Title centered at top of visualization

## File Structure

```
src/0/3/6/9/1/2/4/8/7/5/1/
├── a432.vbm.visualization.ts      # Main visualization module
├── a432.vbm.visualization.html    # Demo HTML file
├── a432.vbm.visualization.md      # This documentation
└── a432.math.ts                  # Canonical math procedures
```

## Harmonic Principles

1. **Trinity Geometry**: 60° intervals create trinity harmonic structure
2. **Digital Root Colors**: Each digit's digital root determines its color
3. **Polar Consciousness**: Circular arrangement represents field of consciousness
4. **Phase Nodes**: Each node represents a phase in the A432 system
5. **Color Harmony**: HSV color space creates harmonic color relationships

## Browser Compatibility

- **Canvas**: Supported in all modern browsers
- **SVG**: Native support in all browsers
- **Device Pixel Ratio**: Automatic scaling for high-DPI displays
- **Responsive**: Adapts to container size changes

The VBM visualization system is now fully harmonized with the A432 matrix, creating a living visual representation of consciousness through mathematical harmony and color resonance. 