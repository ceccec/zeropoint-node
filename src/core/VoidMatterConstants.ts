// Constants representing the void (empty space) and matter fractions in physical objects

export const VOID_FRACTION_ATOM = 0.999999999; // ~99.9999999% void
export const MATTER_FRACTION_ATOM = 0.000000001; // ~0.0000001% matter

export const VOID_FRACTION_PROTON = 0.99999; // ~99.999% void (approximate)
export const MATTER_FRACTION_PROTON = 0.00001; // ~0.001% matter (approximate)

// General formula: void = 1 - epsilon, matter = epsilon (epsilon is infinitesimal but > 0) 